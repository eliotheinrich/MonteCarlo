outputfile=outputt
path=data/test

rm -r $path
mkdir $path

g++ -pthread -I ~/lib -O3 -std=c++11 StiffnessCurve.cpp -o $outputfile

for L in 8; do
    filename=$path/stiffness"$L".txt
    ./$outputfile $filename $L 4
done

rm $outputfile

